import "./CardTransfer.css";

export const CardTransfer = ({ className, ...props }) => {
	return (
		<div className={"solar-card-transfer-linear " + className}>
			<img className="group" src="group0.svg" />
		</div>
	);
};
